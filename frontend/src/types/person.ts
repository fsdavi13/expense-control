export interface Person {
  id: number;
  name: string;
  age: number;
}

export interface CreatePerson {
  name: string;
  age: number;
}

export interface UpdatePerson {
  name: string;
  age: number;
}